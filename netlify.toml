# Netlify deployment config for static portfolio
[build]
  publish = "."
  command = "echo 'Static site, no build step'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
